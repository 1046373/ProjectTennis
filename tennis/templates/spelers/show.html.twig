{% extends 'base.html.twig' %}

{% block title %}Spelers{% endblock %}

{% block body %}
    <h1>Spelers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ speler.id }}</td>
            </tr>
            <tr>
                <th>Voornaam</th>
                <td>{{ speler.Voornaam }}</td>
            </tr>
            <tr>
                <th>Tussenvoegsels</th>
                <td>{{ speler.Tussenvoegsels }}</td>
            </tr>
            <tr>
                <th>Achternaam</th>
                <td>{{ speler.Achternaam }}</td>
            </tr>
            <tr>
                <th>Schoolnaam</th>
                <td>{{ speler.Schoolnaam }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_spelers_index') }}">back to list</a>

    <a href="{{ path('app_spelers_edit', {'id': speler.id}) }}">edit</a>

    {{ include('spelers/_delete_form.html.twig') }}
{% endblock %}
